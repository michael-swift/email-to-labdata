# Minimal Dockerfile for Lambda - only essential dependencies
FROM public.ecr.aws/lambda/python:3.11

# Copy only Lambda requirements (not the full requirements.txt)
COPY lambda_requirements.txt ${LAMBDA_TASK_ROOT}

# Install minimal dependencies
RUN pip install --no-cache-dir -r lambda_requirements.txt

# Copy function code
COPY lambda_function.py ${LAMBDA_TASK_ROOT}

# Verify installations
RUN python -c "import openai; import boto3; print('Dependencies installed successfully')"

# Set the CMD to your handler
CMD ["lambda_function.lambda_handler"]